# Jad-owned ML and inference dependencies only.
# This file is for local classifier inference and golden replay validation.
# Aya should later merge these into the final worker/Docker requirements.
# Install CPU-only PyTorch locally before this file:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# Then install the remaining ML dependencies:
# pip install -r requirements-ml.txt
pillow==12.2.0
pydantic==2.13.4
pytest==9.0.3
